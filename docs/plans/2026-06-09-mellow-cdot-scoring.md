# Chicago Bike Grid — Plan: Mellow + CDOT scoring swap

> **For agentic workers:** implement task-by-task with TDD (RED → GREEN → REFACTOR). Steps use checkbox (`- [ ]`) syntax. Spec: [`docs/specs/2026-06-09-mellow-cdot-scoring-design.md`](../specs/2026-06-09-mellow-cdot-scoring-design.md).

**Goal:** Replace the brokenspoke/PFB LTS source with a stress classification built from the Mellow Bike Map (baseline) + CDOT bike-facility layers (override), on a routing graph built from OpenStreetMap. The 3-tier model, weight tables, router, HIN overlay, POIs, and both views are unchanged.

**Tech stack delta:** add `osmnx` (graph build) + keep `geopandas`/`shapely`/`pyproj`. Remove Docker/brokenspoke. `pytest` + `responses` for tests as today.

**Working state at end of plan:** `make refresh` (no Docker) produces a valid `data/bikemap.db` with `streets.lts` / `intersections.lts_approach` populated from Mellow + CDOT, plus `lts-network.geojson.gz`. `make test` passes clean.

---

## Phase 0 — Setup

- [x] **0a. Worktree.** Created `feat/mellow-cdot-scoring` at `.worktrees/mellow-cdot-scoring`; baseline 176 tests pass.
- [x] **0b. Dependency.** `osmnx>=2.0,<3` already in `requirements.txt`; verified `import osmnx` (2.1.0) in the venv.
- [x] **0c. Discover CDOT endpoints.** Done — see `docs/dataset-ids.md`. Chosen layers: on-street `Bikeway_Network_2024_Final_Public` (field `BIKE_DSPLY`), off-street `Trails_Network_2024_11_18`; both geometry-enabled at `outSR=4326`. Live vocab differs from the original draft (abbreviated single words; no "Signed Bike Route") — §1.1 updated accordingly.

## Phase 1 — Pure classifier (no I/O, fully unit-tested)

- [x] **1a. RED:** `tests/prep/test_tier_classifier.py` — truth table for a new `prep/scoring/classifier.py::classify_tier(mellow_kind, cdot_facility)`. Cases: each Mellow kind alone; each CDOT facility alone; both-agree; both-disagree (CDOT wins); Mellow **path** + worse CDOT (stays tier 1 — the floor); neither (tier 3). Done — 21 RED cases failing on missing module.
- [x] **1b. GREEN:** implement `classify_tier` + the `CDOT_FACILITY_TO_TIER` and `MELLOW_KIND_TO_TIER` tables per design §1.1. **Key the CDOT table on the live `BIKE_DSPLY` vocab** (`PROTECTED`/`NEIGHBORHOOD`→1, `BUFFERED`/`BIKE`→2, `SHARED`→3) — not the title-case strings; see §1.1 / `docs/dataset-ids.md`. Keep it a pure function over strings — no geometry. Done.
- [x] **1c. REFACTOR:** move the facility/kind vocab into module constants; assert exhaustiveness (unknown facility string → baseline, logged). Match case-insensitively / normalize whitespace so the fallback `DISPLAYROU` vocab (`PROTECTED BIKE LANE`, …) also resolves if that layer is ever swapped back in. Done — 32 tests pass, ruff + mypy clean.

## Phase 2 — Source parsers

- [x] **2a. Mellow fetch + parse.** Done — `prep/fetchers/mellow.py` (`MellowFetcher` + `parse_mellow_features` → `MellowFeature(kind, way_ids, slug, name)`); 4 tests pass, ruff + mypy clean. RED: `tests/prep/test_mellow_fetcher.py` with a small fixture mirroring real `mbm.mellowroute` dumpdata records — fields are `slug`, `name`, `bounding_box` (SRID=4326 POLYGON), `type` (kind ∈ street/route/path), and **`ways`: a list of OSM way-ID strings** (the live fixture at `app/mbm/fixtures/mellowroute.json` is a single 117-record file: 39 each of street/route/path; **there is no per-route LineString — geometry is the `ways` list**). GREEN: `prep/fetchers/mellow.py` downloads `mellowroute.json` from `jeancochrane/mellow-bike-map`; `MellowFetcher(Fetcher).fetch(cache_dir)` writes it + returns `FetchResult`, and a parser yields `MellowFeature(kind, way_ids: frozenset[str], slug, name)`. **No reprojection** (no geometry). **Verification:** parses route/street/path into the three kinds with non-empty `way_ids`. **(decision 2026-06-09: way-ID join, not geometry — Mellow→edge matching deferred to Phase 4 keys off `osmid`.)**
- [x] **2b. CDOT facilities fetch + parse.** Done — `prep/fetchers/cdot_facilities.py` (`CdotFacilitiesFetcher` + `parse_cdot_facilities` → `CdotFacility(facility_type, geometry, off_street)`); reuses hin.py's `_esri_to_geojson`; 3 tests pass, ruff + mypy clean. Full prep suite: 123 pass. RED: `tests/prep/test_cdot_facilities_fetcher.py` using a saved FeatureServer JSON fixture. GREEN: `prep/fetchers/cdot_facilities.py` (ArcGIS query, `outSR=4326`, paginated) yields `CdotFacility(facility_type, geometry)` for on-street + off-street trail layers. Mirror the existing `prep/fetchers/hin.py` ArcGIS pattern. **Verification:** facility-type strings map through `CDOT_FACILITY_TO_TIER`.

## Phase 3 — OSM graph builder

> **Design note (2026-06-09):** Phase 3 emits lightweight **`OsmEdge`/`OsmNode`** records (topology only, incl. `length_m` + `osm_way_ids` for the Mellow join), *not* `SegmentRecord` directly. Phase 4 attaches the tier and converts `OsmEdge → SegmentRecord` (with `ft_int_str`/`tf_int_str = None`) so `DbBuilder.insert_streets` still works unchanged (F5 satisfied at the Phase-4 boundary). This keeps Phase 3 free of tier concerns and avoids a frozen-dataclass placeholder-`lts` hack.

- [x] **3a. RED:** `tests/prep/test_osm_graph.py` — feed a tiny saved OSM graph (osmnx graphml fixture or a hand-built MultiDiGraph) and assert `build_street_edges(graph)` yields edge records with: stable `head_node_id`/`tail_node_id`, LineString geom (EPSG:4326), `length_m`, `highway`, `name`. Assert the graph is connected.
- [x] **3b. GREEN — Done.** `prep/graph/osm_builder.py`: `bbox_to_osmnx` (F2 reorder, tested), `build_street_edges` (osmid list-collapse → `osm_id`+`osm_way_ids` F3, reverse-direction dedup, synthesized `road_id` counter, length fallback), `build_nodes`, and the network-bound `build_graph_from_bbox` wrapper (retain_all=False; integration-tested in Phase 6). 7 unit tests pass, ruff + mypy clean. Original text: `osmnx.graph_from_bbox(...)`, simplify, project to 4326, emit edge + node records. **Emit the existing `prep.lts.ingest.SegmentRecord` / `IntersectionRecord` dataclasses** so `DbBuilder.insert_streets` / `insert_intersections` work unchanged (review F5; builder computes `length_m` itself). Synthesize a stable unique int `road_id` per edge; this `road_id` is also the HIN match key (builder.py:75, main.py:361). Leave `ft_int_str`/`tf_int_str` = `None` (intersection tiers come from Phase 4c). **Gotchas (review):** (F2) osmnx 2.x `graph_from_bbox(bbox, *, …)` takes a single tuple ordered `(left, bottom, right, top)` = `(min_lng, min_lat, max_lng, max_lat)` — `target.bbox` is `(min_lat, max_lat, min_lng, max_lng)`, so **reorder before the call**. (F3) `streets.osm_id` is `INTEGER NOT NULL` but simplified osmnx edges carry a **list** of `osmid`s — collapse to a single int (first element). Use osmnx `u`/`v` node ids as `head_int_id`/`tail_int_id`. **Verification:** node IDs at edge ends are consistent (shared between adjacent edges) so the router can traverse. `osmnx.graph_from_bbox(...)`, simplify, project to 4326, emit edge + node records. **Emit the existing `prep.lts.ingest.SegmentRecord` / `IntersectionRecord` dataclasses** so `DbBuilder.insert_streets` / `insert_intersections` work unchanged (review F5; builder computes `length_m` itself). Synthesize a stable unique int `road_id` per edge; this `road_id` is also the HIN match key (builder.py:75, main.py:361). Leave `ft_int_str`/`tf_int_str` = `None` (intersection tiers come from Phase 4c). **Gotchas (review):** (F2) osmnx 2.x `graph_from_bbox(bbox, *, …)` takes a single tuple ordered `(left, bottom, right, top)` = `(min_lng, min_lat, max_lng, max_lat)` — `target.bbox` is `(min_lat, max_lat, min_lng, max_lng)`, so **reorder before the call**. (F3) `streets.osm_id` is `INTEGER NOT NULL` but simplified osmnx edges carry a **list** of `osmid`s — collapse to a single int (first element). Use osmnx `u`/`v` node ids as `head_int_id`/`tail_int_id`. **Verification:** node IDs at edge ends are consistent (shared between adjacent edges) so the router can traverse.

## Phase 4 — Spatial classify (graph × sources)

- [x] **4a. RED — Done.** `tests/prep/test_classify_network.py` — given OSM edges + Mellow features + CDOT facilities (all fixtures), assert each edge gets the correct tier. **Mellow → edge is a way-ID join** (`edge.osmid ∈ mellow_ways[kind]`, edge osmid may be a list); **CDOT → edge is a spatial buffer-match** (reuse the `prep/joins/hin_to_osm.py` approach). Include the Mellow-path-floor case end-to-end.
- [x] **4b. GREEN — Done.** `prep/scoring/classify_network.py` (`classify_network` → `list[SegmentRecord]`): Mellow way-id join (`_build_way_kind_map`/`_mellow_kind_for_edge`, best tier), CDOT spatial match (`_cdot_tier_for_edges`, reuses hin_to_osm `_project`/`_bearing`/`_bearing_diff`, off-street bearing-optional → tier 1), combine via `combine_final_tier`. Classifier refactored to expose `mellow_baseline_tier`/`cdot_tier_for_facility`/`combine_final_tier` (32 classifier tests still green). 3 classify_network tests pass. Original text below: for each edge: Mellow kind from the `way_id -> kind` map (best/min tier on conflict) → baseline; overlapping CDOT facility → override via `classify_tier`; apply path floor. Attach `lts` to each edge record. **(review F7)** The reused `hin_to_osm` matcher applies a ±30° bearing filter — right for on-street CDOT lines, but **off-street trails/paths may not parallel street bearing**, so use a bearing-optional match for the trail layer (off-street → tier 1 regardless, which mostly sidesteps it). **(Mellow no longer uses the spatial matcher — way-ID join only.)**
- [x] **4c. Intersections — Done.** `prep/scoring/intersection_tiers.py`: `lts_approach_for_node` (max incident, floor 1) + `build_intersection_records(nodes, segments)` → `list[IntersectionRecord]`. 3 tests pass; full prep suite 136 pass. Original text below: `prep/scoring/intersection_tiers.py::lts_approach_for_node` = max incident edge tier. Replaces `synthesize_intersections` for the new graph. **(review F6)** Preserve the existing floor: a node with no incident-edge tiers defaults to `lts_approach = 1` (schema is NOT NULL). Node geometry comes from osmnx node coords. **Verification:** node tier = worst incident edge.

## Phase 5 — Pipeline wiring

- [x] **5a. sources.yaml + config_loader — Done.** Removed the `brokenspoke:` block from `sources.yaml`, dropped `BrokenspokeConfig` + the required-section `KeyError` from `config_loader.py`, and pruned `tests/prep/test_config_loader.py` (3 tests pass, ruff+mypy clean). Original text: The `mellow`, `cdot_bike_network`, `cdot_off_street_trails` entries already exist (Phase 0). Remove the `brokenspoke:` block **and** drop `prep/config_loader.py`'s required-`brokenspoke` section (config_loader.py:60 raises if absent — review F4); remove `BrokenspokeConfig` from the dataclass + loader. Update `tests/prep/test_config_loader.py`.
- [x] **5b. main.py (restructure) — Done.** `run_pipeline` now fetches Mellow + CDOT facilities (dropped the brokenspoke runner, `--skip-brokenspoke`, and `brokenspoke_results_dir`); the build step calls `build_graph_from_bbox → build_street_edges/build_nodes → classify_network → build_intersection_records`, then the unchanged HIN join (each edge's synthesized `road_id` as `OsmSegment.osm_id`), POI ingest (osm+cdp), treatments, atomic swap, lts-network export. Also dropped the superseded `cdot_bike_facilities` sanity fetch from main (file removal deferred to 5d). `tests/prep/test_main.py` rewritten (4 pass; happy path asserts tiers `[1,3]` via a hand-built graph + mocked parse). Full suite 226 pass, ruff+mypy clean. Original text: RED: extend `tests/prep/test_main.py`. GREEN: the whole streets/intersections/HIN/POI insert block is nested under `if results_path is not None:` (main.py:343) — replace that gate with the new flow: fetch Mellow + CDOT → `build_street_edges` → `classify_network` → `intersection_tiers`. Drop the step-2 brokenspoke runner and `--skip-brokenspoke`. Keep the HIN join (pass each edge's synthesized `road_id` as `OsmSegment.osm_id`, exactly as today), POI ingest, treatments, atomic swap, `lts-network` export.
- [x] **5c. Delete dead code — Done.** Removed `prep/lts/runner.py`, `prep/lts/synthesize_intersections.py`, `docker/compose.brokenspoke.yml` (docker/ now empty/gone), and their tests (`test_lts_runner.py`, `test_synthesize_intersections.py`). Trimmed `prep/lts/ingest.py` to the record dataclasses (`SegmentRecord`/`IntersectionRecord`/`PoiRecord`) + `ingest_osm_pois`/`ingest_cdp_pois`; dropped `ingest_segments*`, `BROKENSPOKE_POI_FILES`, `ingest_brokenspoke_pois`, `BrokenspokeIngestError`, PFB field constants, `_is_nan`. **Deviation:** kept the record dataclasses in `prep/lts/ingest.py` (already the shared home imported by `DbBuilder` + scoring) rather than moving them — a pure-churn move across ~16 files for zero behavior change; updated the module docstring + field comments to be source-neutral. `test_lts_ingest.py` rewritten (records + OSM/CDP POI ingest; address-composition re-homed onto `ingest_osm_pois`). 210 pass, ruff+mypy clean. README's stale Docker/brokenspoke line deferred to Phase 6. Original text: Remove `prep/lts/runner.py`, `docker/compose.brokenspoke.yml`, brokenspoke refs; trim `prep/lts/ingest.py` to the POI helpers only (drop `ingest_segments*`, `SegmentRecord`/`IntersectionRecord` move to wherever Phase 3 emits them). Update imports.
- [x] **5d. Reporting — Done.** Retitled `lts_diff.py` markdown to "Stress-Tier Regression Diff (Mellow + CDOT)" / "Tier transitions" / "Previous|Current tier" (still diffs `streets.lts`; no logic change). `prep_report.py` builds its source list dynamically, so no code change — updated `test_prep_report.py` fixture names to current sources (`cdot_facilities`, `mellow`). **F8 decision: removed `cdot_sanity`** — deleted `prep/fetchers/cdot_sanity.py` + `test_cdot_sanity_fetcher.py` + the `cdot_bike_facilities` Socrata entry from `sources.yaml`; the ArcGIS `CdotFacilitiesFetcher` (with facility-type field) fully supersedes the cross-check. 208 pass, ruff clean. Original text: Keep `prep/reporting/lts_diff.py` (still diffs `streets.lts`); retitle output to reflect Mellow/CDOT. Update `prep/reporting/prep_report.py` source list. Decide on `cdot_sanity` (Socrata `hvv9-38ut`) fetcher: keep as cross-check or remove (review F8).
- [x] **5e. POI parity — Done.** Expanded `OsmPoisFetcher.CATEGORY_TAGS` to 13: added pharmacy (`amenity=pharmacy`), doctor (`amenity=doctors`), dentist (`amenity=dentist`), university (`amenity=university`), college (`amenity=college`), community_center (`amenity=community_centre`), social_services (`amenity=social_facility`), retail (`shop=True`). Extended `OSM_POI_FILES` in `prep/lts/ingest.py` to the matching 13 `osm_pois_<category>.geojson`→category entries. RED first (`test_osm_pois_fetcher_covers_all_13_categories`, per-file loop, empty-bbox 13-warning assertion all failed), then GREEN. `ingest_brokenspoke_pois` was already removed in 5c. Full suite 209 pass, ruff+mypy clean. In-DB verification deferred to Phase 6. Original text: Removing brokenspoke removes the *input* to `ingest_brokenspoke_pois` (8 categories: pharmacy, doctor, dentist, university, college, community_center, social_services, retail — not covered by the current OSM POI fetcher). Expand `prep/fetchers/pois_osm.py` + `OSM_POI_FILES` to fetch those 8 from OSM (same data brokenspoke repackaged), then drop `ingest_brokenspoke_pois`. RED: extend `tests/prep/test_pois_osm_fetcher.py`. **Verification:** all 13 POI categories present in the built DB.

## Phase 6 — Verify & integrate

- [x] **6a. Done — `make test` fully green** (ruff + mypy + 211 pytest pass). Also renamed two stale `source="brokenspoke"` test-fixture literals → `"osm"`. **Pre-existing `app/core/gap_analysis.py` debt fixed (user request, 2026-06-09):** `make test` lints/types `app/`, and that file had 6 ruff + 2 mypy errors present at the `main` merge-base (commit `719ee74`), unrelated to the scoring change. Initially flagged as a spinoff to keep the PR scoped, but the user asked to fix it here. Fixes (behavior-preserving): removed 3 unused imports (`BaseGeometry`, `unary_union`, `main_weight_for`); C401 set-comprehension; B905 `zip(strict=False)`; SIM108 ternary (comment retained); mypy — added a `None`-guard before indexing a `vertex_for_int_id` result, and renamed a `v` binding that shadowed the `for v in fast.vertex_path` loop var (→ `vtx`) so the `int|None` assignment no longer conflicts with the loop's `int`.
- [x] **6b. Integration build — 2 bugs found & fixed (TDD), then green.** Ran the full fetch→graph→classify→HIN→POI→DB→export path against a ~2 sq-mi Logan Square box (throwaway mini config, since removed). **Bug 1 — `db_build` FAILED:** `_bearing()` in `prep/joins/hin_to_osm.py` called `list(line.coords)`, which raises on multi-part geometries — and CDOT facilities can be `MultiLineString`. Unit tests used single `LineString`s so they missed it. Fix (RED→GREEN): `test_classify_network_handles_multilinestring_facility` reproduced the exact error, then `_bearing` was reworked to flatten any `MultiLineString` into its 2-point sub-segments (type widened `LineString`→`BaseGeometry`). **Bug 2 — repo-root pollution:** osmnx defaults `use_cache=True, cache_folder="./cache"`, so a real build litters the repo root with an un-gitignored `cache/`. Fix (RED→GREEN): `test_build_graph_routes_osmnx_cache_under_data` asserts the builder points the cache under `data/cache/` (gitignored); `build_graph_from_bbox` now sets `ox.settings.cache_folder = "data/cache/osmnx"`. **(Completed after the full-city run revealed a 2nd osmnx call site):** `OsmPoisFetcher.fetch` *also* calls osmnx and runs *before* the graph builder, so it had its own `./cache` write — fixed identically (`test_osm_pois_fetcher_routes_cache_under_data` + `ox.settings.cache_folder` set alongside the existing `requests_timeout`). Both osmnx entry points now redirect; verified no repo-root `cache/` after a build. **Re-run result (OK):** DB built — 2702 streets (tier 274/221/2207 — not all tier-3 ✓), 1697 intersections, 0 dangling endpoints (graph connected ✓), HIN overlay 70 streets/9 ints from 2801 features, 10 OSM + 2 CDP POI categories (hospital/university/college legitimately absent in the small box — full-13 check is 6c), `lts-network.geojson.gz` exported with 4395 features. Full suite 211 pass, ruff+mypy clean.
- [~] **6c. Skipped per user (2026-06-09).** A full-city `make refresh` was started (pure-Python osmnx flow, wrote fetch snapshots to `data/cache/2026-06-09/` and the osmnx HTTP cache to `data/cache/osmnx` — confirming the redirect holds, no repo-root `cache/`), but the user opted not to wait for the full run, so it was terminated mid-download. The 6b mini build already exercised the entire fetch→graph→classify→HIN→POI→DB→export path at small scale and is the binding integration evidence. A real `make refresh` remains the operator's first-run step before deploy.
- [x] **6d. Done.** README setup step now reads "Install Python 3.11+. (No Docker needed …)" with the osmnx/Mellow/CDOT description, replacing the stale brokenspoke-analyzer Docker line.
- [x] **6e. Done.** Reviewed against plan + design (all phases 1–5 + 6a/6b/6d complete; tier direction, schema, weights untouched). **Deploy note (Railway/Render):** the production image (`Dockerfile` + `render.yaml`, `runtime: docker`) is the Flask app runtime and is unchanged by this work — it contains no brokenspoke/PFB references. The only Docker that this change removes was prep-time (`docker/compose.brokenspoke.yml`, deleted in 5c); `make refresh` is now pure-Python (`osmnx`/`geopandas`/`networkx`, already in `requirements.txt`). Net effect: prep no longer needs a Docker daemon, so CI/operator graph builds are simpler; the deploy pipeline (build image → run gunicorn → `upload-db` of `bikemap.db` + `lts-network.geojson.gz`) is otherwise identical. `networkx` is used only by `prep` and arrives guaranteed via `osmnx`, consistent with requirements.txt's "don't pin transitive deps" policy.

## Notes / risks

- **Mellow coverage is partial** (volunteer-mapped, North Side strongest). Streets outside Mellow + CDOT fall to tier 3 by default — expected, but verify it doesn't make routing infeasible in under-mapped areas. The tier *fallback* weights in `routing_weights.yaml` already soften hard cutoffs.
- **Graph keying** is the riskiest change: the HIN join and `lts_diff` both key off `road_id`. **Validated in review:** `DbBuilder.insert_streets` looks up HIN matches by `road_id` (builder.py:75) and `main.py:361` passes `road_id` as `OsmSegment.osm_id`. Phase 3 must synthesize a stable unique int `road_id` per edge and Phase 5b keep that exact wiring; lean on `test_hin_to_osm.py` + `test_lts_diff.py`.
- **Licensing:** Mellow is MIT; CDOT data is public. Add attribution to the README/footer.

## Plan review addenda (2026-06-09, against live code)

Reviewed before Phase 1. Verdict: plan sound; tier direction confirmed (1 = kid-safe, `routing_weights.yaml`), schema/weights untouched. Findings folded into tasks above: **F1** POI parity → new task 5e (keep all 13 categories, user decision); **F2** osmnx 2.x bbox order → 3b; **F3** `osm_id` list-collapse → 3b; **F4** main.py restructure + config_loader `brokenspoke` coupling → 5a/5b; **F5** emit existing `SegmentRecord`/`IntersectionRecord` → 3b; **F6** intersection floor=1 → 4c; **F7** bearing-optional trail match → 4b; **F8** `cdot_sanity` keep/remove → 5d.
